<Window x:Class="Smart_Pacifier___Tool.Temp.Test"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Test" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Button Section -->
        <StackPanel Orientation="Vertical" Grid.Row="0" HorizontalAlignment="Left" Margin="10,10,0,0">
            <Button x:Name="WriteDataButton" Content="Write Data" Width="100" Height="30" Click="OnWriteDataButtonClick" />
            <Button x:Name="ReadDataButton" Content="Read Data" Width="100" Height="30" Click="OnReadDataButtonClick" Margin="0,10,0,0" />
        </StackPanel>

        <!-- Output Section -->
        <TextBox x:Name="ResultsTextBox" Grid.Row="2" Margin="10,10,10,10" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" IsReadOnly="True" />
    </Grid>
</Window>
