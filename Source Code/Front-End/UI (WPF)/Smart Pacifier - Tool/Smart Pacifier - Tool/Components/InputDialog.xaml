<Window x:Class="Smart_Pacifier___Tool.Components.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Smart_Pacifier___Tool.Components"
        mc:Ignorable="d"
        Title="InputDialog" Height="200" Width="400" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title Text -->
        <TextBlock Text="Enter Campaign Name:" Margin="10" VerticalAlignment="Center" HorizontalAlignment="Left" FontWeight="Bold"/>

        <!-- TextBox for Input -->
        <TextBox x:Name="InputTextBox" Grid.Row="1" Margin="10" VerticalAlignment="Center" HorizontalAlignment="Stretch" Height="30"/>

        <!-- Button Panel -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="OK" Width="75" Margin="5" Click="OKButton_Click"/>
            <Button Content="Cancel" Width="75" Margin="5" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
